Add two new fields to gut calls: home_subject and away_subject. Auto-populate both from the fixture's home_team and away_team at the time the gut call is logged — no manual input, no dropdown. Store alongside existing note/tag fields. Display in History/GC as: note text (home_subject), note text (away_subject) — e.g. home lock (Man Utd), away wildcard (Newcastle). No change to By Note table aggregation — it still keys off the raw note string only. No new UI element needed since it's auto-filled from data already present on the fixture.